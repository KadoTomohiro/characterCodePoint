import { UnicodeCategoryPipe } from './unicode-category.pipe';

describe('UnicodeCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new UnicodeCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
